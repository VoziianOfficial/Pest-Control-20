document.querySelectorAll('[data-service-tabs]').forEach(root=>{const photo=root.querySelector('.tabs-photo'),title=root.querySelector('[data-tab-title]'),copy=root.querySelector('[data-tab-copy]'),list=root.querySelector('[data-tab-list]');root.querySelectorAll('.tab').forEach(tab=>tab.addEventListener('click',()=>{root.querySelectorAll('.tab').forEach(t=>t.classList.remove('active'));tab.classList.add('active');const d=JSON.parse(tab.dataset.content);photo.style.backgroundImage=`url('${d.image}')`;title.textContent=d.title;copy.textContent=d.copy;list.innerHTML=d.points.map(x=>`<li>${x}</li>`).join('')}))});
